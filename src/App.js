import { createBrowserHistory } from "history";

export const history = createBrowserHistory();

function App() {
  return (
    <div className="App">
      <p className="text-4xl text-red-500">Test</p>
    </div>
  );
}

export default App;
